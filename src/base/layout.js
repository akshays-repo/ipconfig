import React from "react";
import "./layout.css";

const Layout = ({ getDetails }) => {
  return (
    <div>
      <header className="header header--purple">
        <h1 className="header__title font-display">ALL ABOUT IP</h1>
      </header>
    </div>
  );
};

export default Layout;
